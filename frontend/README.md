# Frontend

La app React Native actualmente se mantiene en la raiz del proyecto por compatibilidad con la configuracion nativa (`android/` y `ios/`).

En una siguiente fase se puede migrar completamente a este directorio `frontend/` si deseas separar por monorepo.
